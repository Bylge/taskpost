# `.env` is never in git — 08-environment.md
.env
.env.*
!.env.example

# Build output and installed dependencies
/vendor
/node_modules
/public/build
/public/hot
/public/storage
/public/fonts-manifest.dev.json

# Runtime and test artefacts
*.log
/storage/*.key
/storage/pail
.phpunit.result.cache
/.phpunit.cache

# Composer credentials — a real secret, not noise
/auth.json

# Editor and OS noise
/.codex
/.cursor/
/.idea
/.nova
/.vscode
/.zed
.phpactor.json
Thumbs.db
.DS_Store

# Commercial and legal notes. Kept out of git so the repository can be public from M1.1
# without a rewrite, for merge gating (06-build-plan.md).
# Still read by agents locally; see docs/private/README.md.
docs/private/
